# 김지상의 이면 — 프로젝트 헌장

이 문서는 이 저장소에서 작업하는 모든 세션(사람이든 Claude든)이 먼저 읽어야 하는
방향성 문서다. 기능을 추가하거나 디자인을 바꾸기 전에, 여기 적힌 목적/톤/원칙에
맞는지 먼저 확인한다.

## 1. 이 사이트는 무엇인가

**개인 블로그 겸 나만의 SNS.** 포트폴리오가 아니다 (포트폴리오는 다른 곳에 올림).

- 요즘 생각하는 것들을 글로 써서 아카이빙한다 — 나중에 스스로 돌아볼 기록이면서,
  동시에 남들도 봤으면 하는 공개된 기록이다.
- 좋아하는 음악·영화·책 등 예술 취향을 드러낸다 (letterboxd, rateyourmusic처럼
  "취향의 큐레이션" 성격).
- 인스타그램/트위터 같은 플랫폼 SNS가 아니라, 내가 완전히 통제하는 나만의 공간에서
  하는 SNS. 알고리즘도, 좋아요 수도, 타인의 UI도 없다.

## 2. 톤 & 목소리

**내밀하고 고백적인 일기 톤.** scaruffi식 건조한 평론 톤이 아니라, 감정과 개인적
생각을 여과 없이 드러내는 쪽. 리뷰(영화/음악)도 평점표보다 "이걸 보고/듣고 내가
무슨 생각을 했는지"가 중심이 되는 게 이 방향에 더 맞는다 — 나중에 리뷰 포맷을
다듬을 때 이 원칙을 기준으로 삼는다.

## 3. 콘텐츠 구조

| 섹션 | 역할 | 우선순위 |
|---|---|---|
| Journal | 일기 · 생각 아카이빙. 사이트의 본체 | 1순위, 가장 자주 씀 |
| Films | 본 영화에 대한 개인적 기록 | 2순위 |
| Music | 들은 음악에 대한 개인적 기록 | 2순위 |
| (Books — 추후 가능성) | 아직 미정 | 보류 |

포트폴리오/이력서성 콘텐츠는 이 사이트에 넣지 않는다.

## 4. 하지 않는 것

- **방명록 / 댓글 기능 없음.** 방문자와의 상호작용은 이 프로젝트에서 불필요하다고
  명시적으로 결정됨. 기존 index.html의 가짜 방명록(새로고침하면 사라지고, 비밀번호
  보호도 클라이언트에서만 도는 눈속임)은 완전히 제거하고 다시 만들지 않는다.
- 좋아요/조회수/방문자 카운터 등 SNS식 참여 지표를 넣지 않는다 — 이건 알고리즘
  없는 나만의 공간이라는 원칙과 상충한다.

## 5. 미학 원칙 — "90년대 감성 유지 + 기능은 현대적으로"

- **유지할 것**: 90년대 개인 홈페이지 특유의 무드 — 두꺼운 테두리, 강한 원색 배색,
  꾸밈없고 손으로 만든 듯한 레이아웃, 방명록 시대의 향수. 세리프/두꺼운 픽셀 느낌
  타이포, 정갈하지 않고 자기만의 방(geocities류) 같은 느낌.
- **버릴 것**: `<font>`, `<center>`, `<dir>`, 깨진 HTML, 반응형 안 되는 고정폭
  테이블, alt 텍스트 없는 이미지, 정의되지 않은 함수 호출, 브라우저 확장 프로그램
  잔재(Grammarly 태그 등).
- **원칙**: 레트로한 "겉모습"과 현대적인 "구조"를 분리한다. 실제 마크업은 유효한
  시맨틱 HTML5 + CSS로 그 무드를 재현하고, 기능(내비게이션, 반응형, 접근성, 로딩
  속도)은 2026년 기준으로 만든다. 즉 "복고풍 스킨을 입은 잘 만들어진 사이트"이지,
  "실제로 낡은 사이트"가 아니다.

## 6. 운영 리듬

**자주 업데이트 — 거의 블로그처럼.** 따라서 콘텐츠 추가가 최대한 가벼워야 한다.
글 하나 올리려고 HTML 태그를 손으로 짜 넣는 지금 방식은 이 리듬과 맞지 않는다.
→ 콘텐츠는 데이터(마크다운/프론트매터)로 분리하고, 레이아웃/템플릿이 자동으로
글 목록·개별 글 페이지를 만들어주는 구조로 간다 (기술 스택은 별도 논의).

## 7. 기술 제약 & 스택

- GitHub Pages 기반 정적 사이트. 별도 서버/DB 없음.
- 방명록을 없앴기 때문에 백엔드가 필요한 기능 자체가 (현재는) 없다.
- **Jekyll 채택 (확정).** GitHub Pages가 네이티브로 빌드해주므로 별도 빌드 도구나
  GitHub Actions 파이프라인이 필요 없다. 글 추가 = 마크다운 파일 + 프론트매터 하나
  작성 후 push.
  - Collections: `_journal/`, `_films/`, `_music/` (책 추가 시 `_books/`).
  - 공통 레이아웃/헤더/푸터로 내비게이션 통일 (지금은 서브페이지에서 홈으로
    돌아갈 방법이 없음 — 반드시 고침).
  - 레트로 무드는 `assets/css/`의 커스텀 CSS로 구현하고, 레이아웃(Liquid)은
    유효한 시맨틱 HTML5로 작성.

---
*이 문서는 프로젝트가 진화하면서 함께 업데이트한다. 방향이 바뀌면 여기부터 고친다.*
